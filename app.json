{
  "name": "Jelly Bot",
  "repository": "https://github.com/RaenonX/Jelly-Bot",
  "env": {
    "MONGO_URL": {
      "description": "Connection string for pymongo to connect to mongoDB.",
      "required": true
    },
    "KEY_CODE": {
      "description": "The field name / key of the cide in the short url record.",
      "required": true
    },
    "KEY_TARGET": {
      "description": "The field name / key of the target in the short url record.",
      "required": true
    },
    "NOT_FOUND_URL": {
      "description": "The url to go if the record cannot be found.",
      "required": true
    }
  }
}